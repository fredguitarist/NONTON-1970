location / {
    proxy_read_timeout 60s;
    client_max_body_size 16m;

    auth_basic $auth;
    auth_basic_user_file /etc/nginx/.htpasswd;

    proxy_pass http://u_front;
    include include/proxy.headers;
}
